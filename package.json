{
  "name": "mehtab-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "netlify dev",
    "test": "node --test",
    "password": "node scripts/password.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "netlify-cli": "^27.5.0"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
